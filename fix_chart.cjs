const fs = require('fs'); let c = fs.readFileSync('src/WebContainer.tsx', 'utf8'); c = c.replace(/(const chart = createChart\([^,]+, \{)/g, '$1\n        handleScroll: { mouseWheel: true, pressedMouseMove: true },\n        handleScale: { mouseWheel: false, pinch: true, axisPressedMouseMove: true },\n        kinematicScroll: { mouse: true },\n'); fs.writeFileSync('src/WebContainer.tsx', c); console.log('Fixed');
