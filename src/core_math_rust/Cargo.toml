[package]
name = "core_math_rust"
version = "0.1.0"
edition = "2021"
description = "Zero-Copy WASM Math Engine for QuantaWeb"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
